 1.Alginvesting es un nuevo proyecto que quiero desarrollar con claude. 
  Dentro de ese directorio hay uno que se llama Alginvesting_base, que es todo lo que tengo hasta ahora (consideralo como 
  solo lectura, esto debe ser parte del gitignore, ya que lo nuevo debe estar desarrollado en "Alginvesting"
  Todo lo prinicipal en lo que estoy trabajando esta en la carpeta scripts, los codigos que empiezan con X0 y X1
  Revisa el proyecto para que creemos el claude.md y el readme
  Como siempre, preguntame todo lo que sea necesario para entender el proyecto, que es lo primero que quiero que hagas 



  [2.] 
  
  Respondo
  1. Están en desarrollo, funcionan y han mejorado, pero ambos siguen con oportunidad de mejorado
  2. Migremos a .py, creo que es mejor. Solo en algunos casos específicos de construcción de módulos que contemplen una componente visual importante, me gustaría quizás partir con ipynb
  3. Por ahora, no lo consideres parte de la base
  4. Este proyecto lo acabo de "migrar de windows a mac". Lo desarrollé en windows, hice un git push y, en el mac hice un git clone (en windows no tengo claude)
     La idea sería ir haciendo ese ir y venir (desarrollar en mac y ejecutar en windows). Ese repo, está dentro de la carpeta Alginvesting_base (todo el repo excepto el gitignore que quedó fuera)
  5. Es un proyecto personal (al menos por ahora)
    6. Los archivos de la carpeta Data/, se actualizan desde el código X0 (al comienzo. Primera etapa). La segunda etapa del código X0 es encontrar soportes y resistencias
  
  PD: Esto debiese estar en las bases de tu funcionamiento, pero quiero que libremente en cualquier momento, actualices el archivo de contexto (y que este incluya los to dos),
  el claude.md y/o algún otro que sea relevante. 

  Además, me gustaría crear alguna skill para hacer push a alguna rama
  en github. Además, cuando se justifique, sugiere crear rama nueva o tambien sugiere en que rama
  dejar los cambios (deja este punto en los to dos para que lo veamos pronto pero no ahora)

  Además, en la metodología de trabajo, cuando planifique, quiero que sugieras 2 o máximo 3 propuestas con una recomendada
  con trade-offs, pros y contras claros. En general, no escribas demasiado en los outputs cosas innecesarias


  Antes de continuar con el to do. Quiero replicar en el repo, la carpeta Data que está en la rama base_v0 (salvo que ya tengas los csvs en el repo y no los haya
  visto). Esto es porque, como se ve en X0.py, cuando se actualizan los csv, hay un limite de registros en mt5, por lo tanto, eso
  se concatena con el archivo ya existente, y despues de un drop duplicates, eso va actualizando los datos (creo que tengo cerca de
  40 días sin actualizar nada para no tener vacíos). Si empiezo esos archivos de nuevo, perderé toda la historia 


## Contextos

i)

  [ ] Evaluar incorporar X2_Intravela al scope (I:6 C:7 H:5 → 0.78)

  Quiero complementar este punto con la siguiente lógica.
  Si hago un backtesting de una configuración específica, me puedo encontrar con el siguiente caso borde
  (es poco probable, pero puede pasar)

  Como solo se ve el bloque de una hora, imaginemos una vela OCHL = (90, 80, 95, 85)
  Si el precio abre en 90, baja a 80 (justo hay un soporte), sube a 95 (activando el trailing stop)
  baja a 85 (en algun momento se cierra la orden con SL).
  Entonces, se abrió y se cerró una orden dentro de la misma vela, y no hay como saberlo a priori (salvo que OCHL cumplan condiciones que lo hagan imposible).
  En la prácica, esto es una orden cerrada que debería quedar registrada.

  El uso de X2, yo lo pensé de la siguiente manera.
  0. Si una vela horaria, cumple con que es imposible abrir y cerrar una orden dentro de ella, continua con normalidad
  1. Si no:
     1a. Tomar un intervalo aleatorio del dataframe del valor por minuto (como se genera, en el código X2. Esto en verdad es lo que se puede rescatar del código) de 60 registros (desde i hasta i +60, donde i es aleatorio)
     1b. Ajustarlo para que, cumpla con el OHCL de la vela horaria: 
     Literalmente, sería como simular el comportamiento minuto a minuto, dentro de una vela horaria cuando es requerido
     Con esto, se podría determinar el comportamiento con mucha más precisión

  Preguntame si tienes dudas

  ii) Para X4, el backtester
  Además, la tupla valor-N, debería tener una fecha maxima
  Esto es porque, logicamente, se espera que en un punto del tiempo t, la estimación de soportes y resistencias se haya hecho con velas que lleguen máximo, hasta t, no después
  En la práctica, la busqueda de mejores soportes /resistencias de valor-N, debería ser un continuo, actualizando delta_inicial, para que la solución quede presionada no solo a estar mas actualizada, si no que a ser mejor

  Considerar ademas que cambié FECHA_INICIAL al 2022-01-01

iii) para DELTA_INICIAL. 
1. Considerar que el delta_inicial en un valor-N puede comenzar alto e ir decreciendo a medida que un delta_fijo, obtiene solución optima (pasa por todas las iteraciones sin mejorar)
Si esto ocurre, el delta puede multiplicarse por un número como 0.7 y guardarse en un json.
Así, cada vez que vuelva a ejecutarse valor-N, se debe leer la mejor ultima solución + delta asociado

2. En general, si un par valor-N encuentra que reemplazando un soporte/resistencia (llamar sopres), por otro, mejora la función objetivo
Entonces esa solución debe guardarse por defecto (si el proceso empieza nuevamente, esa sería la solución inicial)
Esta solución que se va guardando al mejorar, puede ser automáticamente tambien la que entra a mt5. Se reemplaza el sopres por otro si es que es posible
Notar que, cuando se buscan sopres, hay un argumento en la función que nunca ocupé que son las ordenes actualmente activas (es decir, las que están activas en mt5). Estas deben ser parte de sopres FIJOS al momento de ejecutar el algorítmo: Tienen que ir si o si y no pueden moverse